interface Filters {
    purpose : string,
    rentFrequency : string,
    categoryExternalID : string,
    minPrice : string,
    maxPrice : string,
    areaMax : string,
    roomsMin : string,
    bathsMin : string,
    sort : string,
    locationExternalIDs : string,
}